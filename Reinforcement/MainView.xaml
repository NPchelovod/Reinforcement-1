<Window x:Class="Reinforcement.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reinforcement"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:CreateSchedules}" Height="180" Width="240">
    <StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Margin="5" Text= "Марка конструкции: "/>
            <TextBox Text="{Binding ConstructionMark}" Margin="5" Width="70"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Margin="5" Text="ADSK_Назначение вида: "/>
            <TextBox Text="{Binding ViewDestination}" Margin="5" Width="70" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Click="Button_Click" Margin="5" Padding="5" Content="Создать" Width="80" />
            <Button Click="Click_Cancel" Margin="5" Padding="5" Content="Отмена" Width="80" />

        </StackPanel>
    </StackPanel>
</Window>
